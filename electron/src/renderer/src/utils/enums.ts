export enum RenameGeneralStatusType {
    success,
    error
}

export enum RenameErrorType {
    unexpected,
    invalidFileType,
    noExifData,
    invalid_option
}

export enum UpdateStatusType {
    error,
    checking,
    notAvailable,
    downloading,
    ready
}
